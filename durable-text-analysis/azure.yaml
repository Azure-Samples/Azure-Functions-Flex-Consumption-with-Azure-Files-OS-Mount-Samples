# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: azure-files-durable-text-analysis
hooks:
    postdeploy:
      posix:
        shell: sh
        run: ./scripts/post-up.sh
        interactive: true
        continueOnError: false
services:
  processor:
    project: ./src
    language: python
    host: function
